({
	/*theRadioSelected : function(cmp, event, theRadio) {
        console.log('***THE RADIO WAS SET FROM THE HOMEPAGE***');
        cmp.set('v.value', theRadio);
        
        var filterChangedEvent = cmp.getEvent("filterChangedEvent");
        
        filterChangedEvent.setParams({
            "filterName" : 'Type',
            "selectedFilters" : theRadio
        });
        filterChangedEvent.fire();
    }*/
})